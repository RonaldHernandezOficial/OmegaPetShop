import React from 'react';
import Navbar from '../componentes/Navbar';

const Menu = () => {
    return (
        <div class="wrapper">
            <Navbar></Navbar>
        </div>
    );
}

export default Menu;
